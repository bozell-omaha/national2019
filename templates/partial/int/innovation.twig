{#
<section class="overlap quest">
    <div class="guide-wrapper">
        <div class="guide"></div>
    </div>
    <h1 class="overlap__heading">
        {{post.get_field('heading')}}
    </h1>
    <div class="overlap__content">
        <div class="overlap__img" style="background-image: url('{{ TimberImage(post.get_field('image')).src }}')"></div>
        <div class="overlap__copy">
            {{post.get_field('content')}}
        </div>
    </div>
</section> #}