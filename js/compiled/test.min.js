"use strict";

var eventTitles = document.querySelectorAll('.event__title');
var eventCloseBtns = document.querySelectorAll('.mobile-desc__close');
var body = document.body;
var docEl = document.documentElement; //Disable/Enable Scroll Things
// Scroll control functions. Taken from https://stackoverflow.com/a/4770179.

var keys = {
  37: 1,
  38: 1,
  39: 1,
  40: 1
};

var preventDefault = function preventDefault(e) {
  e = e || window.event;
  if (e.preventDefault) e.preventDefault();
  e.returnValue = false;
};

var preventDefaultForScrollKeys = function preventDefaultForScrollKeys(e) {
  if (keys[e.keyCode]) {
    preventDefault(e);
    return false;
  }
};

var disableScroll = function disableScroll() {
  if (window.addEventListener) // older FF
    window.addEventListener('DOMMouseScroll', preventDefault, false);
  window.onwheel = preventDefault; // modern standard

  window.onmousewheel = document.onmousewheel = preventDefault; // older browsers, IE

  window.ontouchmove = preventDefault; // mobile

  document.onkeydown = preventDefaultForScrollKeys;
};

var enableScroll = function enableScroll() {
  if (window.removeEventListener) window.removeEventListener('DOMMouseScroll', preventDefault, false);
  window.onmousewheel = document.onmousewheel = null;
  window.onwheel = null;
  window.ontouchmove = null;
  document.onkeydown = null;
};

eventTitles.forEach(function (title) {
  var description = title.nextElementSibling;
  title.addEventListener('click', function (e) {
    e.stopPropagation;
    description.style.transform = 'none';
    disableScroll();
  });
});
eventCloseBtns.forEach(function (btn) {
  var container = btn.parentElement.parentElement;
  btn.addEventListener('click', function (e) {
    e.stopPropagation;
    container.style.transform = 'translateX(100%)';
    enableScroll();
  });
});
//# sourceMappingURL=test.min.js.map
