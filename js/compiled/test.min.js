"use strict";

var eventTitles = document.querySelectorAll('.event__title');
eventTitles.forEach(function (title) {
  var description = title.nextElementSibling;
  var arrow = title.querySelector('.arrow-forward');
  title.addEventListener('click', function (e) {
    e.stopPropagation; // description.style.maxHeight = description.scrollHeight + 'px';

    if (!title.classList.contains('open')) {
      title.classList.add('open');
      description.style.transform = 'none';
    } else {
      title.classList.remove('open');
      description.style.transform = 'translateX(100%)';
    }
  });
});
//# sourceMappingURL=test.min.js.map
